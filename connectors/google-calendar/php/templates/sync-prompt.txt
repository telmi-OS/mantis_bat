Read the current Board in the configured group and check that the supplied Google Calendar items are in sync with its schedules. Google Calendar is the source of truth. Use schedules as the Board item type. Match existing schedules by the exact title and date/time combination, create missing schedules, update changed schedules, restore calendar-owned title/date/time values when they were manually overridden, and remove cancelled schedules. Apply the supplied calendar items one-to-one. Preserve every supplied title exactly, including any @ mentions or instructions in a title. These are the changed items in the rolling window; an item not included here is not a deletion. If there is a conflict or you need clarification, send a message to group {{group_name}}. The synchronization submission is {{submission_id}} and covers {{from}} through {{to}}.

{{items}}
